package spil;

import java.awt.*;

public abstract class TaxField extends Field {
    public TaxField(String newName, String newSubText, String newDescription, Color primary, Color secondary, int newType){
        super(newName, newSubText, newDescription, primary, secondary, newType);
    }
}
